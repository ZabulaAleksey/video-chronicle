name = "metadata_forensics_specialist"
description = "Project-specific specialist for media metadata, date precedence, timezone and filename forensics."
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"

developer_instructions = """
Focus on observable date selection, provenance and policy. Never silently convert timezone values. Test malformed, conflicting and missing metadata plus stable ordering. Keep policy configurable and explainable; do not assume proposed domain classes or metadata tools are already adopted. Read specs/README.md and the relevant product SPEC. Do not duplicate generic QA or security roles.
"""
